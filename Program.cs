
public class Program
{
    public static void Main(string[] args)
    {
      Console.WriteLine("CPU Scheduling Algorithms Comparison");
      Console.WriteLine("=====================================");

        /*
            This will be the main program that implements each of the algorithms to measure their performances 
            - Algorithms class contains functions for each
            - Generate a list of process using the Process class
            - Run each algorithm on the same list of processes to compare 

            Measure:
            - Average Waiting Time (AWT)
            - Average Turnaround Time (ATAT)
            - CPU Utilization (CPUU)
            - Throughput (Processes Per Second)
            - 
        */

    }
}